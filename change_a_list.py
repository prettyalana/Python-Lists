
numbers = [1, 2, 3, 4, 5, 99, 2600, 300]

# When you call numbers.reverse, it reverses the list but returns none 
# reversed_list = numbers.reverse()

# Use the reversed() function and convert it to a list
reversed_list = list(reversed(numbers))

print(reversed_list)
# If you use the numbers.reverse print numbers to see the reversed list